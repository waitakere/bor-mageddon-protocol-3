export const CHARACTER_STATS = {
    marko_1993: {
        baseSpeed: 280, // Increased from the original 220 for a snappier feel
        maxHealth: 100,
        hitDamage: 12
    },
    darko_1993: {
        baseSpeed: 290, // Maybe Darko is a bit lighter/faster
        maxHealth: 90,
        hitDamage: 10
    },
    maja_1993: {
        baseSpeed: 250, // Maja is the Heavy/Tank, slightly slower
        maxHealth: 130,
        hitDamage: 15
    },
    // Fallback default
    default: {
        baseSpeed: 280,
        maxHealth: 100,
        hitDamage: 12
    }
};